<% unless flash[:error].present? %>
  $('#answer-<%= @answer.id %>').remove();
  
  $('#answers-count').text('This question has <%= j pluralize(@answer.question.answers.count, 'answer') %>');
<% end %>
